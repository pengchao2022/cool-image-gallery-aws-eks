apiVersion: v1
kind: Service
metadata:
  name: frontend-service
  namespace: comic-website
  labels:
    app: frontend
    service: frontend
spec:
  selector:
    app: frontend
  ports:
  - name: http
    port: 80
    targetPort: 80
    protocol: TCP
  type: ClusterIP